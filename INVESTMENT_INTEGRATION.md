# 💰 Интеграция Investment API

## ✅ Что реализовано

Интеграция с инвестиционной платформой вашего одногруппника: https://cvb2.onrender.com

### Функционал:

1. **Работа с ценными бумагами**
   - Получение списка доступных акций
   - Отображение текущих цен

2. **Калькулятор инвестиций**
   - Расчет полной стоимости операции
   - Учет комиссии брокера
   - Планирование покупок

3. **Управление операциями**
   - Создание операций с уведомлениями
   - Привязка email для триггеров
   - История операций

4. **Система триггеров**
   - Установка целевых цен
   - Автоматические уведомления
   - Мониторинг активных ордеров

---

## 📡 API Эндпоинты

### 1. Получить список ценных бумаг
```http
GET /api/investment/securities
```

**Ответ:**
```json
{
  "success": true,
  "securities": [
    {
      "id": 1,
      "name": "Apple Inc.",
      "price": 170.50
    }
  ],
  "count": 5
}
```

### 2. Рассчитать стоимость операции
```http
POST /api/investment/calculate
Content-Type: application/json

{
  "securityId": 1,
  "quantity": 10,
  "purchasePricePerShare": 170.50,
  "commission": 5.00
}
```

**Ответ:**
```json
{
  "success": true,
  "calculation": {
    "totalCost": 1710.00,
    "stocksCost": 1705.00,
    "commission": 5.00,
    "quantity": 10
  }
}
```

### 3. Создать операцию с уведомлением
```http
POST /api/investment/operations
Content-Type: application/json

{
  "securityId": 1,
  "quantity": 10,
  "purchasePricePerShare": 170.50,
  "commission": 5.00,
  "clientEmail": "client@example.com"
}
```

**Ответ:**
```json
{
  "success": true,
  "operation": {
    "id": 123,
    "securityId": 1,
    "totalCost": 1710.00,
    "email": "client@example.com"
  }
}
```

### 4. Создать триггер
```http
POST /api/investment/triggers
Content-Type: application/json

{
  "operationId": 123,
  "targetPrice": 160.00,
  "triggerType": "BELOW"
}
```

### 5. Проверить триггеры
```http
POST /api/investment/triggers/check
```

### 6. Тест подключения
```http
GET /api/investment/test
```

---

## 🌐 Веб-интерфейс

### Вкладка "Инвестиции"

1. **Откройте** http://localhost:3000
2. **Перейдите** на вкладку "Инвестиции"
3. **Нажмите** "Тест подключения Investment API"
4. **Загрузите** список ценных бумаг

### Возможности:

✅ **Просмотр акций** - список с текущими ценами
✅ **Калькулятор** - расчет стоимости с комиссией
✅ **Создание операций** - с email для уведомлений
✅ **Визуализация** - красивое отображение данных

---

## 🎯 Сценарии использования

### Сценарий 1: "Покупка товара + инвестиция"

Клиент покупает технику и одновременно инвестирует в акции производителя:

```
1. Клиент выбирает Samsung Galaxy (25,000₽)
   ↓
2. Система предлагает: "Инвестируйте в Samsung!"
   ↓
3. Калькулятор показывает: 10 акций Samsung = 12,000₽
   ↓
4. Клиент создает операцию + заказ товара
   ↓
5. Устанавливает триггер: при цене <1,100₽ → купить еще
   ↓
6. Получает уведомление на email при срабатывании
```

### Сценарий 2: "Кешбэк акциями"

Программа лояльности через инвестиции:

```
Заказ на 50,000₽ → кешбэк 5% = 2,500₽
    ↓
Вместо денег → покупка акций на 2,500₽
    ↓
Триггер: при росте на 20% → уведомление
    ↓
Клиент продает и получает прибыль
```

### Сценарий 3: "Инвестиционный план для гаджетов"

Накопление на дорогую технику через акции:

```
Цель: iPhone за 120,000₽ через год
    ↓
Ежемесячно: покупка акций Apple на 10,000₽
    ↓
Триггеры: уведомления о росте/падении
    ↓
Через год: продажа акций и покупка iPhone со скидкой
```

### Сценарий 4: "Мониторинг для бизнеса"

Владелец магазина отслеживает акции поставщиков:

```
Триггер: Apple упала на 10%
    ↓
Уведомление: "Время закупаться!"
    ↓
Увеличение закупок при выгодных ценах акций
```

---

## 🔧 Техническая информация

### Структура файлов

```
├── investment-service.js    # Модуль интеграции
├── server.js                 # API эндпоинты /api/investment/*
├── public/index.html         # Вкладка "Инвестиции"
└── INVESTMENT_INTEGRATION.md # Эта документация
```

### Зависимости

Используется уже установленный **axios** (добавлен для СДЭК API).

### Настройка

По умолчанию используется production URL: `https://cvb2.onrender.com/api`

Можно изменить через переменную окружения:
```bash
set INVESTMENT_API_URL=https://your-custom-url.com/api
```

---

## 🔗 Интеграция двух API

### Магазин электроники + Инвестиции + Доставка

Полный цикл работы с клиентом:

```
1️⃣ ТОВАРЫ (Ваш API)
   ↓ Клиент выбирает товары
   
2️⃣ ИНВЕСТИЦИИ (Investment API)
   ↓ Предлагаем акции производителя
   ↓ Создаем операцию с триггером
   
3️⃣ ДОСТАВКА (СДЭК API)
   ↓ Рассчитываем стоимость доставки
   ↓ Создаем заказ в СДЭК
   
4️⃣ ЗАКАЗ (Ваш API)
   ↓ Сохраняем все в БД
   ↓ Уменьшаем остатки
   
5️⃣ УВЕДОМЛЕНИЯ (Investment API)
   ↓ Email при срабатывании триггеров
```

### Пример комплексного запроса:

```javascript
// 1. Создать заказ с товарами
const order = await createOrder({
    clientName: "Иван",
    clientEmail: "ivan@example.com",
    items: [{productId: 1, quantity: 2}]
});

// 2. Рассчитать доставку
const delivery = await calculateDelivery({
    city: "Санкт-Петербург",
    weight: 1000
});

// 3. Создать инвестиционную операцию
const investment = await createInvestment({
    securityId: 5, // Apple
    quantity: 5,
    purchasePricePerShare: 170.50,
    clientEmail: "ivan@example.com"
});

// 4. Установить триггер
const trigger = await createTrigger({
    operationId: investment.id,
    targetPrice: 165.00,
    triggerType: "BELOW"
});

// Итого: Заказ + Доставка + Инвестиция + Триггер = Полный сервис!
```

---

## 💡 Примеры запросов

### cURL: Получить бумаги
```bash
curl http://localhost:3000/api/investment/securities
```

### cURL: Расчет операции
```bash
curl -X POST http://localhost:3000/api/investment/calculate \
  -H "Content-Type: application/json" \
  -d '{
    "securityId": 1,
    "quantity": 10,
    "purchasePricePerShare": 170.50,
    "commission": 5.00
  }'
```

### JavaScript: Создание операции
```javascript
const result = await fetch('/api/investment/operations', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
        securityId: 1,
        quantity: 10,
        purchasePricePerShare: 170.50,
        commission: 5.00,
        clientEmail: 'client@example.com'
    })
});

const operation = await result.json();
console.log('Операция создана:', operation.id);
```

---

## ✅ Статус интеграции

- ✅ Получение списка ценных бумаг
- ✅ Расчет стоимости операций
- ✅ Создание операций с email
- ✅ Управление триггерами
- ✅ Проверка триггеров
- ✅ Веб-интерфейс
- ✅ API документация (Swagger)
- ✅ Интеграция с магазином
- ✅ Интеграция с доставкой

---

## 📞 Полезные ссылки

- **Investment API**: https://cvb2.onrender.com
- **Ваш магазин**: http://localhost:3000
- **Swagger документация**: http://localhost:3000/api-docs

---

**Интеграция Investment API полностью функциональна!** 🎉

Теперь ваш магазин электроники предлагает:
- 🛍️ Покупку товаров
- 🚚 Доставку СДЭК
- 💰 Инвестиции в акции производителей
- 🔔 Уведомления о триггерах

**Полный цикл обслуживания клиента!**

